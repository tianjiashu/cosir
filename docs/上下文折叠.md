审查汇总：前端与后端契约对齐
5 个子 Agent 分别审查模型域、任务/工作区域、轮次/附件/工具域、事件/日志/变更域、API 路由层。整体结论：共享契约（shared/ts）字段定义基本对齐，但存在后端路由/响应未落地导致的系统性悬空 + 数处类型/字段偏差。

🔴 必须修（功能性断裂，前端必崩或必 404）
#	问题	归属	位置
1	GET /providers、POST /providers/{id}/discover、POST /providers/{id}/models、PUT/DELETE /models/{id} 后端均未注册路由，前端 api.ts/ProviderSettingsDialog/ProviderModelsSection 调用必 404	后端	providers_api.py / models_api.py 缺端点
2	GET /agents 后端完全不存在（仅响应模型定义，无 agents_api.py），agents.ts 的 AgentProfileResponse/default_agent_id 是悬空契约	后端	缺 agents_api.py
3	后端 models_api.py:48-59 构造 ModelEntryResponse 时漏传必填 supports_video（schema 第44行必填），list_models 端点必然 ValidationError 500，前端 listModels() 必败	后端	models_api.py
4	WorkspaceResponse.workspace_id 前端 string vs 后端 int，且 api.ts:CreateTaskRequest.workspace_id: number 与 useTask.createTask(workspaceId: string) 隐式 string→number 漂移	前端	workspace.ts:11 / api.ts:38 / useTask.ts:257
🟡 应修（契约偏差，当前未崩但语义错位）
#	问题	归属	位置
5	ModelEntryRecord.provider_id: string vs 后端 provider_id: int	前端	model.ts:147
6	supports_video 后端已返回前端未收（model.ts 缺字段）	前端	model.ts
7	ModelCreateRequest 前端含 temperature/top_p/max_tokens?，后端不接收（请求侧臆造）	前端	model.ts:191-201
8	events.ts 头部声称由 generate_runtime_event_ts.py 生成，但脚本在仓库不存在（仅 generate_openapi.py），整份实为手维护副本，"勿手改"注释误导	文档/链路	events.ts:1-8
9	events.ts 把 workspace_preparing/ready/degraded 塞进 RuntimeEvent 信封（强制 task_id），但后端 workspace 事件走独立 WorkspaceEvent（无 task_id）→ 类型错配	前端	events.ts:36-39/253-305
10	logs.ts LogError 前端定死 {type,message,stack}，后端 error 是自由 dict 无 schema 约束	前后端	logs.ts:31-35
11	WorkspaceState 前端无 failed/accepted（后端 WorkspacePrepareResponse.state 允许）	前端	workspaceEvent.ts
12	SSE 收到 409（非 pending turn）前端未回退历史列表，仅抛通用错误	前端	sseConnectionBase.ts:125
⚪ 存量漂移（仅登记，不强制改）
model_id/display_name/max_context_window/temperature/top_p/max_tokens 前端期望但后端 /models 未投影（但 ModelSelector 强依赖 model_id/display_name 作 key/label —— 运行时断裂风险高，建议优先推动后端补齐投影或前端兜底默认值）。
✅ 已确认对齐（无需动）
reasoning_effort 本次新增完全对齐；CreateTurnRequest 无 agent_id、字段与 reasoning_effort: str|None 对齐；ToolCallFinishedPayload.retryable 拼写六字段正确；TurnStatus/TurnRecord/AttachmentRef/ToolDisplayPayload 全部对齐；TaskRecord 无 agent_id 残留、CreateTaskRequest 仅 text/workspace_id；RuntimeEvent 信封字段、changes.ts 全部对齐；SSE event 名一致。
主 Agent 裁决与建议
结构性根因：本次审查暴露的最大问题不是「前端写错」，而是后端大量契约未落地（路由缺失 + 响应构造漏字段）。这违背项目「第零铁律」——长期迭代中前端悬空契约会持续制造 404 与崩溃。

我建议按以下顺序分两个 PR 推进（请你拍板是否授权我落地）：

PR-A（后端补齐，消除悬空） — 由开发 Agent 实现：

providers_api.py 补 GET /providers + POST .../discover + POST .../models
models_api.py 补 PUT/DELETE /models/{id} + 修复 supports_video 漏传 + 投影 model_id/display_name/max_context_window（至少前两项，解除 ModelSelector 运行断裂）
新建 agents_api.py + GET /agents
PR-B（前端契约收敛） — 由开发 Agent 实现：

#4/#5/#6/#7/#9/#11/#12 类型与字段修正
#8 将 events.ts 头部改为「手维护契约，对齐 runtime_event_payload_registry + RuntimeEvent.to_dict()」并删误导注释（或补真实生成脚本，符合第零铁律长期最优）
是否授权我按 PR-A / PR-B 启动独立开发 + 审查 + 测试闭环？还是你只想先修其中某几类（如仅前端 #4–#7/#9/#11/#12，后端悬空路由暂不动）？