# 本文件只是模板。运行时配置只有两个生效位置（env_files() 只读这两处，其余位置一律不生效）：
#   - 桌面应用（含 tauri dev）：<APPDATA>\com.cosir.desktop\.cosir\.env（Tauri app_data_dir()，Roaming）
#   - 绕过 Tauri 直跑后端：<repository>/.cosir/.env
# 注意：apps/backend/.env 不会被任何代码读取（历史上误放在此处的配置一律不生效，已删除）。
# 同名 .env.local 会覆盖 .env 中的同键；改完需重启应用（provider 在工具注册期构造）。

# Langfuse 可观测性（详见 docs/Langfuse可观测性集成技术方案.md）
CODING_AGENT_LANGFUSE_ENABLED=false
CODING_AGENT_LANGFUSE_PUBLIC_KEY=
CODING_AGENT_LANGFUSE_SECRET_KEY=
CODING_AGENT_LANGFUSE_BASE_URL=http://124.220.55.187

# 代理：无需在此声明。系统代理由后端自动探测（Windows 注册表），总开关为 CODING_AGENT_PROXY_AUTO_DETECT（默认开）。
# 历史上为 litellm 提供的 CODING_AGENT_WEB_PROXY_URL / _API_KEY 已随 litellm（2026-09-19 移除）作废，代码零引用，不要再用。
