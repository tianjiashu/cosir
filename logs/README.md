# Logs

The backend writes local diagnostic logs to `logs/app.log` by default.

Do not commit generated log files. Keep this directory present so the log location is predictable during development and debugging.
